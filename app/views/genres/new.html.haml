= form_for @genre do |f|
  = f.text_field :name, class: "mv-genre-form__input", placeholder: "ジャンルを入力してください"
  = f.collection_check_boxes :user_ids, User.all, :id, :name
  = f.submit "Save", class: "mv-genre-form__action-btn"